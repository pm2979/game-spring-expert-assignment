# convention.md

Java / Spring Boot 코드 작성 규칙입니다. 저장소 전체에 적용됩니다.

## Java 작성 원칙

- 들여쓰기는 공백 4칸을 사용하고, 중괄호는 현재 프로젝트의 Java 스타일을 따른다. 요청과 관계없는 파일을 일괄 포맷팅하지 않는다.
- 클래스·인터페이스·enum은 `PascalCase`, 메서드·필드·지역 변수·매개변수는 `camelCase`, 상수는 `UPPER_SNAKE_CASE`를 사용한다.
- 패키지명은 소문자를 사용하고 기존 패키지 구조를 유지한다.
- 파일명은 원칙적으로 파일의 주요 타입명과 일치시킨다.
- 답변, 코드 주석, 직접 작성하는 학습용 로그는 한국어로 작성하고 코드 식별자는 영어로 작성한다.
- 외부 공개가 필요한 경우가 아니라면 mutable `public` 필드를 만들지 않는다.
- 생성자 주입에 사용하는 의존성은 가능한 경우 `final`로 유지한다.
- 하드코딩을 무조건 상수로 분리하지 않는다. 반복되는 정책이나 의미 있는 값만 분리하고, API 명세를 한눈에 확인하기 쉬운 경로·검증 값까지 불필요하게 추상화하지 않는다.
- DRY는 동일한 정책이나 지식이 실제로 반복될 때 적용한다. 한 번만 사용되는 과제 로직을 미래 재사용을 예상해 미리 추상화하지 않는다.
- 요청과 관련 없는 기존 코드는 스타일을 포함해 수정하지 않는다. 요청 범위를 벗어난 이름 변경, 정렬, 포맷팅, 리팩터링을 함께 수행하지 않는다.
- `ASSIGNMENT.md`에서 제공된 클래스명, 패키지, 메서드 형태와 코드는 특별한 이유가 없으면 유지한다.

## Spring 계층 및 DI

- Controller, Service, Repository를 분리한 3 Layer Architecture를 유지한다.
- **Controller**는 HTTP 요청 매핑, 입력 검증, Service 호출, HTTP 응답 생성에 집중한다.
- **Service**는 비즈니스 규칙, 트랜잭션 경계, Entity와 DTO 변환 흐름을 담당한다.
- **Repository**는 JPA를 통한 데이터 조회·저장·삭제에 집중한다.
- Spring Bean 의존성은 생성자 주입을 우선한다. 기존 프로젝트가 Lombok의 `@RequiredArgsConstructor`를 사용한다면 같은 방식을 유지한다.
- 필드 주입(`@Autowired`를 필드에 직접 선언)은 새로 추가하지 않는다.
- 단순히 계층을 나누기 위한 목적만으로 실제 책임이 없는 클래스나 인터페이스를 추가하지 않는다.

## API / DTO

- API 경로, HTTP Method, JSON 필드명, 필드 타입, enum 값은 `ASSIGNMENT.md`와 API 명세에 정확히 맞춘다.
- 요청 DTO는 `~Request`, 응답 DTO는 `~Response` 이름을 사용한다.
- Controller에서 요청 DTO 검증이 필요한 경우 `@Valid`를 사용하고, 검증 조건은 DTO의 Bean Validation 어노테이션으로 표현한다.
- `@NotBlank`, `@NotNull`, `@NotEmpty` 등은 요구사항에 맞는 의미를 확인한 뒤 사용하고 임의로 서로 대체하지 않는다.
- Entity를 API 응답으로 직접 노출하지 않고 요구사항에 맞는 Response DTO로 변환한다.
- DTO에는 API 응답·요청에 필요한 필드만 둔다. 편의를 위해 Entity 전체나 사용하지 않는 필드를 추가하지 않는다.
- `ResponseEntity`와 성공 상태 코드는 제공된 코드 및 API 명세의 범위를 따른다.
- 외부 API 응답 DTO는 실제 JSON 구조와 필드명이 일치하도록 만들며, 과제에서 `@Getter`만 요구한 클래스에 불필요한 동작을 추가하지 않는다.

## JPA / Entity / Transaction

- 엔티티 연관관계는 단방향으로만 사용한다.
- `cascade`, `orphanRemoval`, 양방향 컬렉션을 사용하지 않는다.
- 자식 `RunCard`의 조회·저장·삭제는 Repository를 통해 명시적으로 처리한다.
- Entity의 기존 이름, 필드, enum 의미와 제공된 메서드를 임의로 바꾸지 않는다.
- 조회 전용 메서드에는 필요에 따라 `@Transactional(readOnly = true)`를 사용할 수 있지만, 저장·수정·삭제가 일어나는 메서드에는 `readOnly = true`를 사용하지 않는다.
- 과제에서 더티 체킹을 요구하는 수정은 영속 상태 Entity의 변경 메서드를 사용하고, 불필요한 `save()` 호출로 요구 방식을 바꾸지 않는다.
- Repository 쿼리 메서드는 Spring Data JPA의 명명 규칙을 사용해야 하는 단계에서는 그 규칙을 우선 사용한다.
- N+1 최적화나 JPQL `group by`, DTO projection은 해당 요구사항이 있는 단계에서 적용하고 이전 단계에 미리 넣지 않는다.
- 과제에서 명시한 쿼리 수, 정렬 순서, 조회 순서는 결과뿐 아니라 실제 SQL 로그로 검증할 수 있게 작성한다.

## 예외 처리 및 검증

- 예외는 현재 과제 단계에서 요구하는 방식만 적용한다. 다음 Lv에서 제공되는 예외 구조를 미리 도입하지 않는다.
- 없는 데이터, 이미 종료된 게임 등 도메인 조건은 Service에서 판단하고 현재 단계에서 지정한 예외를 사용한다.
- 전역 예외 처리가 요구되는 단계에서는 `@RestControllerAdvice`와 제공된 `ErrorResponse` 형식을 유지한다.
- 예외를 잡아서 무시하거나 정상 응답으로 바꾸지 않는다. 원인을 확인할 수 있도록 서버 로그와 HTTP 상태 코드를 보존한다.
- 입력 검증은 Controller 진입 지점에서 처리하고, 비즈니스 규칙 검증과 구분한다.

## 컬렉션 / 정렬 / 가독성

- 컬렉션 변환과 정렬은 요구사항을 코드에서 바로 읽을 수 있게 작성한다.
- 여러 정렬 기준이 필요한 경우 `Comparator`의 비교 순서를 요구사항과 같은 순서로 표현한다.
- Stream API, 반복문, Map 중 무엇을 사용하든 현재 학습 단계에서 이해하기 쉬운 방식을 우선한다.
- 한 줄로 지나치게 많은 연산을 연결해 디버깅이 어려워지는 코드는 피한다.
- 성능 최적화는 실제 요구사항이나 확인된 문제를 기준으로 한다. 과제에서 요구하지 않은 캐시, 병렬 처리, 복잡한 자료구조를 미리 추가하지 않는다.

## 주석

- 주석은 한국어로 작성한다.
- 직접 구현하거나 수정한 주요 메서드에는 목적과 책임을 이해할 수 있는 짧은 설명을 작성한다.
- 추가 주석은 코드의 동작을 그대로 풀이하지 않고, **Spring 동작 원리, 설계 의도, 전제 조건, 제약 사항, 주의가 필요한 예외**가 있을 때만 간결하게 작성한다.
- `@Transactional`, JPA 더티 체킹, Bean 등록처럼 코드만 보고 의도가 바로 드러나지 않는 학습 포인트는 필요하면 짧게 이유를 남긴다.
- 실행되지 않는 주석 코드를 남기지 않는다.
- 외부 문서화가 실제로 필요한 공개 API가 아니라면 불필요한 Javadoc을 추가하지 않는다.

## 변경 범위와 검증

- 현재 작업 중인 Lv와 직접 관련된 파일만 수정한다.
- 다음 Lv의 TODO, 도전 기능, 최적화를 편의상 함께 구현하지 않는다.
- 컴파일 성공만으로 기능 완료로 판단하지 않는다. `ASSIGNMENT.md`의 해당 단계 `확인` 항목을 최종 기준으로 사용한다.
- 수정 후 가능한 범위에서 애플리케이션 실행, HTTP 요청, DB 데이터, 서버 로그, SQL 로그를 실제로 확인한다.
- 검증하지 못한 항목은 완료라고 표현하지 않는다.
