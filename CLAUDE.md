# CLAUDE.md
## 기본 원칙

- `ASSIGNMENT.md`를 이 프로젝트의 최우선 요구사항으로 사용한다. 요구사항과 다른 방식으로 임의 구현하지 않는다.
- 이 프로젝트의 목적은 과제를 빠르게 완성하는 것이 아니라, **요구사항을 이해하고 코드를 작성한 뒤 실제 실행 결과로 동작을 확인하며 Spring을 학습하는 것**이다.
- 한 번에 과제 전체를 자동 구현하지 않는다. 현재 작업 중인 Lv 또는 사용자가 지정한 범위만 진행하고, 다음 Lv의 기능을 미리 구현하지 않는다.
- 요구사항을 만족하는 최소한의 변경만 한다. 요청받지 않은 기능·추상화·유연성을 추가하지 않는다.
- 요청 범위 밖의 리팩터링을 하지 않는다. 변경으로 생긴 미사용 코드만 정리한다.
- 작업을 검증 가능한 목표로 바꾸고(예: 에러 재현 → 원인 확인 → 수정 → 같은 동작 재실행), **실제 검증 전까지 완료로 보고하지 않는다.**
- 가정은 명시하고, 코드·로그·명세에서 확인할 수 있는 내용은 먼저 확인한다. 모르는 내용은 추측해서 채우지 않는다.

## 프로젝트 환경

- Spring Boot 기반 Java 백엔드 학습 과제 프로젝트
- Gradle, Spring MVC, Spring Data JPA, Validation, MySQL(Docker)을 중심으로 사용한다.
- 프로젝트에 포함된 프론트엔드는 백엔드 API의 실제 동작을 검증하는 용도로 사용한다.
- Java, Spring Boot, Gradle 및 라이브러리 버전은 **현재 프로젝트 설정을 그대로 따른다.** 필요하지 않은 의존성을 임의로 추가하거나 버전을 변경하지 않는다.
- 구현·리뷰 시 단순히 정답 코드를 만드는 것보다 **왜 문제가 발생했고, Spring이 어떻게 동작하며, 수정 후 무엇으로 정상 동작을 확인할 수 있는지**를 함께 판단한다.

## 작업 전 읽기 - 관련 부분만

- `ASSIGNMENT.md` - 현재 진행하는 Lv의 요구사항, 제공 코드, 확인 방법
- `convention.md` - Java/Spring 코드 작성 규칙
- 현재 Lv와 직접 관련된 `src/main/java`, `src/main/resources` 파일만 확인한다.

`ASSIGNMENT.md`와 코드가 충돌하면 `ASSIGNMENT.md`를 우선한다. 코드 작성 규칙은 `convention.md`를 따른다.

현재 단계에 필요한 부분만 읽고 시작하며, 매 작업마다 프로젝트 전체를 탐색하거나 다음 Lv까지 미리 분석하지 않는다.

## 응답·소통 톤

- 한국어로 답변한다.
- 코드 수정 전, **현재 문제와 수정 의도**를 한 줄로 먼저 설명한다.
- 명세와 다른 방향이 필요해 보이면 차이를 먼저 알리고 임의로 우회하지 않는다.
- 모르는 것은 추측하지 말고 `모름` 또는 `현재 정보만으로 확인 불가`라고 명확히 말한다.
- 요청한 것만 만든다. 사용하지 않을 설정, 미래 확장용 구조, 과도한 예외 처리나 추상화를 미리 추가하지 않는다.
- 정상 동작하는 인접 코드, 이름, 서식은 요청과 관계없으면 수정하지 않는다.
- `ASSIGNMENT.md`에 그대로 사용하라고 제공된 코드는 특별한 이유가 없으면 형태를 임의로 바꾸지 않는다.

## Plan Mode 중심 작업

- 이 프로젝트는 **Auto Mode로 과제를 한 번에 완성하는 방식보다 Plan Mode 중심으로 진행한다.**
- Plan Mode에서는 코드를 수정하기 전에 현재 단계의 문제를 이해할 수 있도록 계획을 먼저 작성한다.
- 계획은 읽기 쉽고 구조적으로 작성하며, 가능한 경우 표를 사용하고 구현 순서를 명확히 표시한다.
- Plan에는 가능하면 다음 내용을 포함한다.
  1. 현재 Lv의 목표와 완료 조건
  2. 먼저 재현하거나 확인할 현재 증상
  3. 관련 Spring 개념과 확인해야 할 이유
  4. 확인할 파일과 수정할 파일
  5. 구현 순서와 핵심 코드 변경 미리보기
  6. 수정 후 실행할 명령·API 호출·DB/로그/화면 확인 방법
- Plan Mode에서는 정답 코드만 바로 제시해 학습 과정을 건너뛰지 않는다. 에러 메시지, 현재 코드, API 명세를 근거로 원인을 찾는 과정을 우선한다.
- 실제 구현 단계에서도 **현재 Lv만** 수정한다. 다음 단계의 TODO나 도전 기능을 편의상 함께 구현하지 않는다.

## 공통 작업 규칙

- 기본 작업 흐름은 **요구사항 확인 → 실패/현재 상태 재현 → 원인 파악 → 최소 구현 → 실행 → 결과 비교 → 학습 내용 정리** 순서로 진행한다.
- `ASSIGNMENT.md`에서 실패 로그를 먼저 확인하라고 한 단계는 수정부터 하지 말고 실제 실패를 재현해 원인 메시지를 확인한다.
- API 경로, HTTP Method, JSON 필드명, 타입, enum 값은 API 명세와 정확히 일치시킨다.
- 제공된 기존 클래스 이름과 패키지는 바꾸지 않는다.
- Controller, Service, Repository를 분리한 3 Layer Architecture를 유지한다.
- CRUD 필수 기능은 과제 요구사항대로 Docker MySQL과 JPA를 사용한다.
- 프론트엔드가 기대한 대로 동작하지 않을 때 서버 요구사항을 우회하기 위해 프론트엔드를 수정하지 않는다. 먼저 API 응답과 서버 로그를 확인한다.
- 과제에서 직접 검색하고 학습하라고 명시한 주제는 결과만 적용하지 말고, 구현 전에 핵심 개념과 현재 문제에 필요한 이유를 짧게 설명한다.

## 네이밍 (= convention)

- 계층 클래스: 역할을 드러내는 접미사를 사용한다. (`GameController`, `GameService`, `GameRepository`)
- 요청 DTO: `~Request` (`RunCardRequest`, `ProgressRequest`)
- 응답 DTO: `~Response` (`CardResponse`, `GameSummaryResponse`)
- 예외: `~Exception` (`GameNotFoundException`, `GameFinishedException`)
- 상수: `UPPER_SNAKE_CASE`

## 코드 규약

- 상세 Java/Spring 작성 규칙은 **`convention.md`** 를 따른다.
- 코드는 과제 학습 목적에 맞게 읽기 쉽게 작성하고, 가독성·유지보수성을 고려하되 현재 요구사항보다 앞선 확장성이나 최적화를 미리 구현하지 않는다.

## Spring / JPA 제약

- 엔티티 연관관계는 `ASSIGNMENT.md` 요구사항대로 **단방향만 사용한다.**
- `cascade`, `orphanRemoval`, 양방향 컬렉션을 추가하지 않는다.
- 자식 엔티티 `RunCard`의 조회·저장·삭제는 Repository를 통해 명시적으로 처리한다.
- 조회용 트랜잭션과 쓰기 트랜잭션의 차이를 구분한다. 데이터를 변경하는 메서드에 `@Transactional(readOnly = true)`를 사용하지 않는다.
- 이름 변경 등 과제에서 더티 체킹을 요구하는 작업은 임의의 다른 저장 방식으로 대체하지 않는다.
- N+1, `group by`, DTO projection 등 도전 단계의 최적화는 해당 Lv에 도달하기 전에 선행 구현하지 않는다.

## API / DTO 규칙

- Controller는 HTTP 요청·검증·응답 연결에 집중하고, 비즈니스 규칙은 Service에 둔다.
- Repository는 데이터 조회·저장 책임에 집중한다.
- 요청/응답 DTO는 API 명세의 필드명과 타입을 정확히 맞춘다.
- 요청 검증은 현재 단계의 명세에 맞게 Bean Validation을 사용하고 Controller에서 `@Valid` 적용 여부를 확인한다.
- Entity를 편의상 그대로 API 응답으로 노출하지 않는다. 과제에서 요구하는 Response DTO로 변환한다.
- 성공 상태 코드는 과제에서 허용한 범위와 제공된 코드 형태를 따른다.

## 검증 및 보고

- **코드를 작성한 것만으로 완료 처리하지 않는다.** 현재 Lv의 `확인` 항목을 실제 검증 기준으로 사용한다.
- 가장 작은 범위부터 검증한다. 필요에 따라 `./gradlew bootRun`, 관련 테스트, HTTP 요청, 서버 로그, MySQL 데이터, Hibernate SQL 로그를 확인한다.
- API는 가능하면 실제 요청을 보내 상태 코드와 JSON 응답을 확인한다.
- DB 저장·수정·삭제 요구사항은 가능하면 실제 데이터 변경 여부까지 확인한다.
- 화면 동작이 완료 조건인 단계는 서버가 실행된 상태에서 실제 게임 흐름을 확인한다. 실행 환경상 화면 확인이 불가능하면 검증하지 않은 것으로 명확히 보고한다.
- 오류가 발생하면 바로 우회하지 말고 에러 메시지와 스택트레이스에서 현재 작업과 관련된 원인을 먼저 찾는다.
- 완료 보고에는 다음만 짧게 정리한다.
  - 변경 파일
  - 구현한 요구사항
  - 실행한 검증과 결과
  - 이번 단계에서 이해해야 할 Spring 핵심 개념
  - 미수행 검증 또는 남은 문제
- 커밋 메시지는 Conventional Commits 형식을 사용한다.
